(function( $ ){
	
	$.fn.stripes = function(options) {
		// stripe me
	};
	
	$.fn.stripesAdd = function(){
		// add the stripes
	}

	$.fn.stripesClear = function(){
		// clear the stripes
	}	
	
})( jQuery )